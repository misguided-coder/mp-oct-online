1) Run the App in eclipse using the following maven goal.
   liberty:dev 
   
2) Just Hit the below URL in the browser.
   http://localhost:9080/index.html

3) Check Push Events are coming from Server to Client automatically to make MS reactive and very reponsive to client.